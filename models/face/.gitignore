.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Per-driver calibration profiles — personal biometrics, not project code.
profiles/*.json
!profiles/.gitkeep

# Camera intrinsics from calibrate_camera.py — specific to one physical camera.
camera_calibration.json

# Generated test clip — rebuild with tests/make_test_video.py.
tests/assets/static_test.mp4

# Per-session artifacts
*.csv
*.log
bench_*.json
snapshot_*.png
